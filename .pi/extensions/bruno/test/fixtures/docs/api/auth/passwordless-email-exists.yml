info:
  name: Check Email Exists
  type: http
  seq: 2

http:
  method: GET
  url: "{{process.env.APP_BASE_URL}}/api/v1/auth/passwordless/email/exists"
  headers:
    - name: content-type
      value: application/json
  body:
    type: json
    data: "{}"

runtime:
  scripts:
    - type: after-response
      code: bru.runner.setNextRequest("Create Anonymous Session");
  assertions: []

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
